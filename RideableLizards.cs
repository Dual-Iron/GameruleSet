namespace GameruleSet
{
    public class RideableLizards
    {
        private readonly Rules rules;

        public RideableLizards(Rules rules)
        {
            this.rules = rules;
        }
    }
}